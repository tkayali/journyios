MOB.Journy.mJourny = Backbone.Model.extend({
	idAttribute: "id",

	initialize: function() {
		//clean up data here?
	}
});

MOB.Journy.mEntry = Backbone.Model.extend({
	idAttribute: "id",

	initialize: function() {
		//clean up data here?
	}
});